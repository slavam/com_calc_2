%div{ class:"row"}
  %aside{ class:"col-md-4"}
    %section{ class:"user_info"}
      = render 'payer'
  %div{ class:"col-md-8"}
    %h3 Счета
    - if @flat.accounts.any? 
      #accounts
      #accounts_data{flatId: @flat.id.to_json, flat: @flat.to_json, accounts: @accounts.to_json, utilities: @utilities.to_json, categories: @categories.to_json, tariffs: @tariffs.to_json}
      = javascript_pack_tag 'account/accounts_by_flat'
